# HyperLiquid Testnet 快速测试指南

## 前提条件

✅ 您已经完成：
- 访问 https://app.hyperliquid-testnet.xyz
- 连接钱包 `0xYOUR_WALLET_ADDRESS_HERE`
- 领取 testnet faucet（已有 949 USDC）
- 钱包已激活并可以在 UI 上交易

## 一、快速配置步骤

### 1. 确认 `.env` 文件

确保 `.env` 文件包含您的私钥：

```bash
# HyperLiquid Configuration
HYPERLIQUID_PRIVATE_KEY=your_private_key_here  # 不带 0x 前缀
```

⚠️ **重要**: 私钥不要带 `0x` 前缀

### 2. 确认 `config.yaml` 设置

```yaml
# Environment: 'testnet' or 'mainnet'
environment: 'testnet'

# Testnet configuration (only used when environment='testnet')
testnet:
  hyperliquid:
    base_url: 'https://api.hyperliquid-testnet.xyz'

  # Risk limits for testnet (more relaxed)
  risk:
    max_position_size_usdc: 1000.0
    max_leverage: 5
    max_daily_loss_usdc: 200.0
    max_concurrent_positions: 3
```

✅ 确保 `environment: 'testnet'`

## 二、测试选项

### 选项 1: 直接测试订单执行（最快）

运行单个订单测试：

```bash
python test_order_placement.py
```

**测试内容**：
- ✅ 获取当前 BTC 市场价格
- ✅ 下一个小额限价单（0.001 BTC）
- ✅ 立即取消订单
- ✅ 验证钱包连接和 API 签名

**预期输出**：
```
======================================================================
Direct Order Placement Test
======================================================================

Wallet: 0xYOUR_WALLET_ADDRESS_HERE
Testnet: https://app.hyperliquid-testnet.xyz

Attempting to place a small test order...
Current BTC mid price: $95,597.00
Test order price: $95,490.00 (rounded to $10 tick)

======================================================================
SUCCESS!
Order ID: 43143951987

Trying to cancel...
Order cancelled successfully
======================================================================
```

### 选项 2: 完整的 Testnet 连接测试

运行完整的连接和数据测试：

```bash
python test_testnet_connection.py
```

**测试内容**：
- ✅ 测试市场数据获取（BTC, ETH, SOL）
- ✅ 测试账户认证和余额查询
- ✅ 测试 K线数据获取
- ✅ 验证所有 API 端点

### 选项 3: 完整的交易功能测试

运行完整的交易流程测试：

```bash
python test_testnet_trading.py
```

**测试内容**：
- ✅ 多币种限价单（BTC, ETH, SOL）
- ✅ 订单取消功能
- ✅ 杠杆更新功能
- ✅ 错误处理

⚠️ **注意**: 这个测试会实际下单，请确保理解测试内容

### 选项 4: 集成测试（DRY-RUN 模式）

运行完整的集成测试套件（安全模式）：

```bash
python run_integration_tests.py --fast
```

**测试内容**：
- ✅ 28 个集成测试（全部在 DRY-RUN 模式）
- ✅ 数据收集功能
- ✅ AI 决策流程
- ✅ 交易执行模拟
- ✅ 零风险（不会实际下单）

**预期输出**：
```
================================================================================
Running Integration Tests (Dry-Run Mode)
================================================================================

Test Configuration:
   [+] Mode: DRY-RUN (Safe - No real trades)
   [+] Market Data: REAL (from HyperLiquid API)
   [+] Trade Execution: SIMULATED
   [+] Risk Level: ZERO

================================================================================
28 passed, 2 skipped, 2 deselected in 23.88s
================================================================================
[SUCCESS] All tests passed!
================================================================================
```

## 三、理解测试结果

### ✅ 成功标志

1. **订单成功**：
   ```
   SUCCESS!
   Order ID: 43143951987
   ```

2. **钱包识别**：
   - 看到您的钱包地址 `0xYOUR_WALLET_ADDRESS_HERE`
   - **不会**看到随机地址（如果看到说明签名有问题）

3. **价格有效**：
   - 订单价格在市场价格的 ±20% 范围内
   - 价格已自动四舍五入到 tick size（BTC = $10）

### ❌ 常见错误

1. **"User does not exist"**：
   - ❌ 私钥错误或格式不对
   - ✅ 解决：检查 `.env` 中的私钥，不要带 `0x` 前缀

2. **"Order price cannot be more than 80% away from reference price"**：
   - ❌ 订单价格偏离市场价格太远
   - ✅ 解决：测试脚本已自动处理，确保使用最新版本

3. **"Price must be divisible by tick size"**：
   - ❌ 价格没有对齐到正确的 tick size
   - ✅ 解决：SDK 已自动处理 tick size 四舍五入

## 四、在 UI 上验证

1. 访问 https://app.hyperliquid-testnet.xyz
2. 连接您的钱包
3. 查看 "Orders" 标签页
4. 您应该能看到测试订单（如果还没取消的话）

## 五、下一步

### 如果测试成功 ✅

您可以：

1. **运行完整策略测试**：
   ```bash
   # 在 DRY-RUN 模式下运行完整的 AI 决策和交易流程
   python run_integration_tests.py
   ```

2. **修改测试参数**：
   - 编辑 `test_order_placement.py` 尝试不同的币种和金额
   - 编辑 `config.yaml` 调整风险参数

3. **切换到 Mainnet**（谨慎）：
   - 在 `config.yaml` 中设置 `environment: 'mainnet'`
   - 确保理解所有风险
   - 建议先在小额资金上测试

### 如果测试失败 ❌

1. 检查错误信息
2. 确认 `.env` 配置正确
3. 确认 `config.yaml` 中 `environment: 'testnet'`
4. 查看 testnet UI 确认钱包已激活
5. 运行 `python verify_wallet.py` 确认钱包地址

## 六、测试脚本说明

| 脚本 | 用途 | 风险 | 推荐 |
|------|------|------|------|
| `test_order_placement.py` | 快速订单测试 | ⚠️ 低（实际下单但立即取消） | ✅ 推荐 |
| `test_testnet_connection.py` | 连接和数据测试 | ✅ 无（只读数据） | ✅ 推荐 |
| `test_testnet_trading.py` | 完整交易功能 | ⚠️ 中（实际下单和杠杆操作） | ⚠️ 谨慎 |
| `run_integration_tests.py --fast` | DRY-RUN 集成测试 | ✅ 无（模拟模式） | ✅ 推荐 |
| `verify_wallet.py` | 钱包地址验证 | ✅ 无（只显示地址） | ✅ 推荐 |

## 七、关键技术细节

### Tick Size 说明

不同币种有不同的 tick size：
- **BTC/ETH** ($1000+): $10 tick
- **中等价格** ($100-$1000): $1 tick
- **低价** ($10-$100): $0.1 tick
- **更低价** ($1-$10): $0.01 tick

SDK 会自动处理价格四舍五入。

### 订单大小限制

- **最小订单**: 各币种不同，通常 BTC = 0.001
- **最大订单**: 受账户余额和风险限制约束

### 杠杆限制

- **BTC 最大**: 40x
- **测试建议**: 5x 或更低
- **风险提醒**: 高杠杆 = 高风险

## 八、问题排查

### 私钥格式检查

```bash
python verify_wallet.py
```

应该显示：
```
Wallet Address: 0xYOUR_WALLET_ADDRESS_HERE
```

### 查看实时日志

脚本运行时会显示详细日志，包括：
- API 请求和响应
- 订单状态变化
- 错误详情

### 查看测试覆盖率

```bash
pytest tests/integration/ --cov=src/trading_bot --cov-report=html
```

## 九、安全提醒

⚠️ **永远不要**：
- 将私钥提交到 git
- 在公开渠道分享私钥
- 在不理解的情况下运行脚本

✅ **始终确保**：
- `.env` 在 `.gitignore` 中
- 先在 testnet 测试
- 理解每个测试的作用

---

**准备好了吗？** 从 `python test_order_placement.py` 开始！🚀
